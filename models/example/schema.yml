version: 2

sources:
  - name: note_source
    database: NOTE1
    schema: NOTE2

    tables:
      - name: EMP

        columns:
          - name: EMP_ID
            tests:
              - unique
              - not_null

          - name: EMP_NAME
            tests:
              - not_null